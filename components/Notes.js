//Component Name should be CamelCase--About,Home,Contact
//Pages Name should be start with small letter--home,about,contact


    {/* <Row className="mt-3">
                <Col md={8}>
                  <p className=" m-2 fw-bold ">Select Quantity:- </p>
                </Col>
                <Col
                  md={4}
                  className="text-end d-flex justify-content-end align-items-center "
                >
                  <Button
                    variant="warning"
                    className="p-2"
                    onClick={() => {
                      setQuantity(quantity > 1 ? quantity - 1 : 1);
                    }}
                    disabled={quantity === 1}
                  >
                    <FontAwesomeIcon icon={faMinus} />
                  </Button>

                  <span className="mx-3">{quantity}</span>
                  <Button
                    variant="warning"
                    className="p-2"
                    onClick={() => {
                      setQuantity(quantity < 20 ? quantity + 1 : 10);
                    }}
                    disabled={quantity === 20}
                  >
                    <FontAwesomeIcon icon={faPlus} />
                  </Button>
                </Col>
              </Row> */}